let  divide= function() {
    return (2000/100);
  }
let square=x=x*x;
let parameter=[1,2,3];  
let squares = parameter.map(x => x*x);
return square;
let add = (parameter1, parameter2) => parameter1 + parameter2;
add(2,3); 